export interface UserRow {
  id: string
  email: string
  display_name: string
  created_at: number
  is_active: boolean
}

export interface User {
  id: string
  email: string
  displayName: string
  createdAt: number
  isActive: boolean
}
